# Changesets

Run `pnpm changeset` when a change should produce a package release. Select the affected packages, choose the appropriate semantic version bump, and describe the change.

Run `pnpm version-packages` when preparing a release to apply pending changesets and update package changelogs.
